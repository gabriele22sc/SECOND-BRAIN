`ACTION VFX` per effetti video

`ALT+DRAG` per duplicare una clip all'interno della timeline;

`A` premuto per spostare nella timeline tutte le clip a DESTRA del puntatore oppure `SHIFT+A` per spostare le clip a SINISTRA